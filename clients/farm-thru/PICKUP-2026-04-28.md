# FarmThru pickup — 2026-04-28

**Context**: 2026-04-27 was a heavy compliance + UX day. ALL 5 PICKUP-2026-04-27 tasks shipped, plus 7 ad-hoc fixes (LP variant swap, mobile sticky-CTA clearance, CSF font 11→16px, Resend throttle, scheduler region fix, universal CSF rule, production templates). Scheduler is currently **PAUSED** (manual action — see "Open items" below for when to resume).

**This pickup**: 2 user-facing follow-ups (Stripe rebrand, scheduler resume) + 2 engineering follow-ups (writer regression, demoted seeds) + 2 NEW from the SMS PR #101 (canonical-SMS vs ADV-001 decision, rubric max_score audit). SMS scoring itself is **DONE** ✅ — both messages passed strong_draft + compliance.

---

## ⚠️ State of the world (DO NOT redo)

### Shipped today (2026-04-27)

**learning-loop:** PRs #94, #95, #96, #97, #98, #99 — Tasks 3-5 of PICKUP-2026-04-27 + universal CSF rule (ADV-001 now scopes meta-ad, landing-page, email, social-post, **sms**) + 8/8 emails pass gate + 13/18 meta-ads strong_draft+ + scored output + review HTMLs at `clients/farm-thru/loop/{scored,review}-pass3-{metaads,emails}.{json,html}` + old "not financial advice" wording stripped from `config.json` (was steering writer LLM).

**sales-skill:** PRs #215 (LP variant o → story section), #216 (apply_fmth_pass2_copy.py canonical), #217 (Resend throttle — 0.55s min interval inside `_send_email`), #218 (mobile sticky-CTA footer clearance, 100px), #219 (CSF disclaimer 11px→14px), #220 (14px→16px), #221 (7 production drip templates + welcome + thank-you + 16/17 LP VIP-card disclaimer all canonicalized; SMS infra confirmed manual-only; check_15 validator updated).

**Ops:** `fmth-drip-hourly` scheduler **PAUSED** in `us-central1` (correct region — au-southeast1 was a doc bug, fixed in PICKUP-2026-04-27 verification log). Resume: `gcloud scheduler jobs resume fmth-drip-hourly --location=us-central1`.

### What's compliant + live

- All 17 LP variants serve the canonical CSF safe-harbour line in 2-3 places (in-card + footer + JS swap, varies by variant)
- All 7 production drip email templates carry the canonical line + clickable Birchal link
- ADV-001 hard-gates email/LP/meta-ad/SMS for missing safe-harbour
- Mobile sticky CTA no longer obscures the CSF disclaimer (100px footer padding); disclaimer font 16px

---

## Open items (in priority order)

### 1. Stripe rebrand — waiting on FMTH keys (USER ACTION REQUIRED)

**Diagnosis (done):** the Stripe account backing prod (`acct_1KPyhPFDEswok1IN`, registered to `jeremy@launcherlab.com.au`) has `business_profile.name = "Jeremy Beasley"` — that's what customers see in Stripe Checkout + receipts. It's the Launcher Lab account, not a FarmThru-owned one.

**Path forward (decided 2026-04-27):** FarmThru sets up their OWN Stripe account, sends keys, we rotate. Steps for FMTH:
1. Sign up at stripe.com/au with FMTH ABN + business address (or use existing FMTH Stripe if they have one)
2. Connect FMTH bank account
3. Set in **Settings → Public details:** business name = "FarmThru", display name = "FarmThru", statement descriptor = "FARMTHRU" (≤22 chars)
4. Set in **Settings → Branding:** FarmThru logo (`Farmthru_he.webp`), primary `#5c8e61`, accent `#257a47`
5. Generate API key (restricted: Checkout Sessions write, Payment Intents read, Customers write, Webhooks read — or a standard secret key)
6. Send: secret key (`sk_live_...`) + webhook signing secret

**Once keys arrive (10 min):**
1. `gcloud secrets versions add stripe-farmthru-secret-key --data-file=-` (paste new key — single-key rotation, NOT --set-secrets)
2. `gcloud secrets versions add stripe-farmthru-webhook-secret --data-file=-`
3. Register webhook in FMTH Dashboard: `https://proposals.launcherlab.com.au/stripe/webhook` event `checkout.session.completed`
4. Cloud Run auto-picks new secret version (uses `:latest`)
5. Test with $5 deposit → confirm receipt shows "FarmThru" + money lands in FMTH bank
6. Refund the test
7. Resume scheduler (see item 2)

**Gotchas:**
- VIP deposits already taken under LL account stay there → LL writes a bank transfer to FMTH for the total VIP balance; refunds for those existing VIPs still go via OLD key (keep old keys around)
- Don't rebrand the LL Stripe account directly — it might also process LL Blueprint payments
- New Stripe accounts may need 1-2 business days verification

### 2. Resume scheduler

**Currently paused** because user wanted Stripe sorted before any new VIP-deposit emails go out. Resume after Stripe rebrand verified working.

```bash
gcloud scheduler jobs resume fmth-drip-hourly --location=us-central1
```

Then trigger one cycle and verify zero 429s + zero compliance-gate rejections:
```bash
gcloud scheduler jobs run fmth-drip-hourly --location=us-central1
gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="proposal-server" AND timestamp>="<TS>"' --limit=20
```

### 3. Writer regression — meta-ad hill-climb candidates score 0.000

From PICKUP-2026-04-27 Task 5 finding #1: every meta-ad variant the writer generated during hill-climb (75 attempts across improve/mutate/targeted/crossover/wildcard) scored 0.000. PR #95 surfaced the no-`$` rule via `_append_content_type_rules()` but candidates still fail — likely missing the CSF safe-harbour or hitting another rule despite the prompt surfacing.

**Investigation steps:**
1. Run hill_climb on a single meta-ad with verbose logging to see what variant the writer generated and what rule failed
2. Check `writer.py::_build_rules_summary()` output for content_type=meta-ad — confirm safe-harbour is in the surfaced rule list
3. If yes, the LLM is just ignoring it — strengthen the prompt language
4. If no, fix `_append_content_type_rules()` for meta-ad

### 4. Demoted seeds: BR-103 + BR-109

From PICKUP-2026-04-27 Task 5 finding #2: BR-103 + BR-109 went from passing in Task 3 (pre-Task-4 rules) to rewrite verdict (0.000) in Task 5 batch (post-Task-4 stricter rules).

**Investigation steps:**
1. `python3 run.py score --client farm-thru --ad clients/farm-thru/loop/meta-ads/BR-103.json` — get the failing rule
2. Same for BR-109
3. Either fix the copy to comply OR document why exemption applies

### 5. SMS scoring — DONE 2026-04-27 PM2 (PR #101)

✅ Both SMS messages scored **strong_draft + compliance PASS**, no hill-climb required:
- `SMS-VIP-OPEN`: composite **0.7722**, verdict strong_draft
- `SMS-VIP-CONFIRM`: composite **0.7833**, verdict strong_draft

Engine now SMS-aware end-to-end (rubric, scorer, llm_judge, writer, batch/hill-climb/review-html scripts, learnings-sms.md at 1974/2000 chars). Full details in PICKUP-2026-04-27.md "Follow-up PM2" entry. Outputs at `clients/farm-thru/loop/{scored,review}-pass4-sms.{json,html}` + seeds at `clients/farm-thru/loop/sms/SMS-VIP-{OPEN,CONFIRM}.json`.

### 6. **Decision needed**: COPY-PACKAGE.md SMS bodies vs ADV-001 (NEW from PR #101)

The 2 canonical SMS bodies in `CSF-VIP-COPY-PACKAGE.md` lines 76-87 **would hard-fail ADV-001 as written** — they lack the safe-harbour trigger phrase "general CSF risk warning". The compliance-clean seed JSONs in `clients/farm-thru/loop/sms/` differ from the canonical markdown.

**Two paths — user picks:**
- **A.** Revise `CSF-VIP-COPY-PACKAGE.md` lines 76-87 to match the compliance-clean seeds (160-char budget honoured: round-opens 89→104 chars, purchase-confirm 140 chars). Cleanest; canonical doc and live SMS stay in sync.
- **B.** Re-design ADV-001's per-channel acceptance: e.g., for SMS, "a Birchal URL alone satisfies safe-harbour" (the URL routes the recipient to a page that contains the full warning). Keeps the original short SMS wording but loosens the deterministic gate.

**Recommendation:** **A** — keeps the gate strict and avoids per-channel exemption sprawl. The "See general CSF risk warning" phrase fits inside the 160-char single-segment budget.

### 7. **Audit needed**: rubric `max_score` values (NEW from PR #101)

Initial SMS rubric had `max_score: 33.0` but the real sum-of-weighted-dims ceiling was 45.0 — surfaced as composites > 1.0 on first scoring run. Fixed for SMS in PR #101.

**One-shot audit:** verify `max_score` in `shared/rubrics/{email,landing-page,meta-ad}.json` matches `sum(dim.weight × dim.max_score)` actually computed by `engine/rubric_scorer.py`. ~15 min — write a small script that loads each rubric, sums weighted maxes, asserts equal to declared `max_score`. Run as a CI check thereafter.

---

## Verification log

(Agent fills as work resumes)

- [ ] Stripe FMTH keys received + rotated + tested
- [ ] Scheduler resumed + first cycle verified clean
- [ ] Writer regression diagnosed + fixed
- [ ] BR-103 + BR-109 either fixed or documented as exempt
- [x] SMS scoring complete — PR #101 merged, both messages strong_draft+compliance PASS, no hill-climb needed (see PICKUP-2026-04-27 PM2 log for engine wiring details)
- [ ] **Decision**: COPY-PACKAGE.md SMS bodies — revise to match compliance-clean seeds (recommended) OR re-design ADV-001 per-channel
- [ ] Rubric `max_score` audit (email/landing-page/meta-ad) — one small script + CI check

---

## Kickoff prompt (paste into fresh Claude tomorrow)

```
Read /Users/jb/Documents/GitHub/learning-loop/clients/farm-thru/PICKUP-2026-04-28.md
and /Users/jb/Documents/GitHub/learning-loop/clients/farm-thru/PICKUP-2026-04-27.md
(read 04-27 second — its verification log has the latest SMS-scoring outcome).

For Stripe: only proceed once I've confirmed FMTH keys are in hand
(I'll paste them here).

For scheduler: don't resume until Stripe is verified working.

For writer regression + demoted seeds: investigate first, propose fix
plan, get my OK, then implement.

For COPY-PACKAGE.md SMS vs ADV-001 (item 6): present the two paths,
get my pick, then implement.

For rubric max_score audit (item 7): just do it — small engineering hygiene.

Default to autonomy on the rest. Use general-purpose agents with
isolation: worktree for code changes. Squash-merge PRs.
```

---

## Don't forget

- Compliance gate (`compliance.enabled: true` in `clients/farm-thru/config.json`) is LIVE on all FMTH content generation. ANY new copy through the writer goes through the hard gate. If it fails, fix the copy, NEVER loosen the gate.
- `--update-secrets` for single-key Secret Manager rotations (NEVER `--set-secrets` — wipes the entire secret mapping)
- All learning-loop scripts run from repo root with `python3 scripts/...`
- Always create feature branches; never commit to main
