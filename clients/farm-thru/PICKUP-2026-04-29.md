# FarmThru pickup — 2026-04-29

**Context**: 2026-04-28 was a heavy structural day. VIP scheme archived end-to-end (recoverable via manifest), thank-you page rebuilt with FB+IG comment-and-tag CTA (live in production), waitlist test data cleaned + renumbered 1..127 contiguously + counter reset + prevention filter shipped + verified by canary, Birchal v5 proposal drafted (Pre-launch Supporter Pack as thank-you upsell, $10 for $20 discount code), email to Jane Ingram on user's clipboard ready to send.

**This pickup**: 1 user-facing follow-up (send the Jane email, watch for her doc comments) + 3 small carryover cleanups + open Stripe rebrand item.

---

## 2026-04-28 PM session update (appended pre-compact)

**Shipped (sales-skill, all squash-merged + Cloud Run auto-deployed):**

| PR | What | Notes |
|---|---|---|
| #235 | `?v=gtmtest` variant — isolated GTM container `GTM-53CLCC8X` | New `web/campaigns/FMTH/index-gtmtest.html` (copy of `index.html`); `TEST_VARIANTS` set in `app.py` is reachable via `?v=` only, never randomly assigned; per-variant GTM override applied in-memory (on-disk `meta.json` untouched) |
| #236 | gtmtest signups skip ALL lead capture | Short-circuit in `campaign_signup` before any side effects: no Firestore, no counter, no Sheet, no welcome email, no server-side CAPI/GA4, no Slack. Returns mock `position:1, total:1, ref_code:"test"`. Client-side GTM still fires from the browser |
| #237 | FMTH email watertight audit + cleanup | New `scripts/audit_fmth_emails.py` reviewer (11 rules); fixed VIP/deposit refs in `drip_nonvip_2.html`, `drip_nonvip_3.html`, welcome email in `web/campaign_emails.py`. 19 → 0 violations. Audit report at `clients/farm-thru/email-audit-2026-04-28.md` |

**Verified in production 2026-04-28**:
- gtmtest URL `https://join.farmthru.com.au/campaigns/fmth-ecb582/?v=gtmtest` returns 200 with `GTM-53CLCC8X`; default + a–q variants still serve `GTM-KWQQ6H9D`
- gtmtest signup posts produce mock response and zero Firestore writes (proven by submitting same email twice → no dedupe response, both got the same mock)
- variant=a signup with `@launcherlab.com.au` email writes to Firestore correctly (proven via dup-check on second submission), is `is_test=True`, excluded from public counter (174 ≠ raw 206)
- Counter ticked from 206 → 212 during user's window — real leads flowing in normally

**⚠️ Caveat**: Short URL `https://join.farmthru.com.au/?v=gtmtest` does NOT work — root redirect to `/campaigns/fmth-ecb582/` drops the query string. Use the long URL.

**Email auditor reuse**: run `python3 sales-skill/scripts/audit_fmth_emails.py` before any future FMTH email template edit. Could become a pre-commit hook.

**New flags from this session (worth picking up):**

- **LP copy bug**: `sales-skill/web/campaigns/FMTH/meta.json` has `PRODUCT_STAT_2_VALUE: "Sydney, Central Coast, Wollongong"` under label `"Delivery Areas"`. FMTH is hub-and-collect — should be "Pickup Areas" or "Hub Locations". Single-line fix; renders on every LP variant. **Default-to-autonomy candidate post-compact.**
- **Carryover #3c dead VIP JS** in thank-you template: confirmed still present after PR #237 (the email audit didn't touch JS). Already on the existing carryover list — bundle with #3a/#3b.

**Verification log additions (mark complete):**
- ✅ FMTH email queue cleaned + auditable — drip queue is watertight before scheduler resume
- ⏳ Scheduler resume still pending Stripe (#2 / #3 below)
- ⏳ User running their own GTM container validation on `?v=gtmtest` — once green, `meta.json:gtm_id` swap is a one-line change. Pause for user's call on dual-container vs cutover.

---

## State of the world (DO NOT redo)

### Shipped 2026-04-28

**learning-loop**: PRs #116 (waitlist renumber + audit), #118 (renumber overnight verify), #120 (V3 proposal short rewrite), #121 (V4 Supporter Pack thank-you upsell), #122 (V5 discount code framing).

**sales-skill**: PRs #228 (replace thank-you content with FB+IG CTA), #229 (clickable post card).

### What's compliant + live

- All 17 LP variants serve the canonical CSF safe-harbour line + have VIP card commented out
- Thank-you page now uses FB+IG comment-and-tag CTA (no per-user `ref_code` referral UI)
- VIP scheme gated behind `FMTH_VIP_ENABLED = False` in `app.py`
- Waitlist DB / Sheet1 / LP counter all = 127 (positions 1..127 contiguous)
- `is_test=True` prevention filter on signup endpoint excludes test emails from counter / drips / sheet sync
- 7 non-target Sheet tabs verified byte-identical (or structurally identical for Supermetrics feeds)
- Cloud Scheduler `fmth-drip-hourly` PAUSED in `us-central1`

---

## Open items (in priority order)

### 1. Send the Jane email (USER ACTION)

User has the email on clipboard. Plain text + RTF versions both saved at `/tmp/jane-reply.txt` + `/tmp/jane-reply.rtf`. The email links to the Google Doc proposal (https://docs.google.com/document/d/16FT7CMdXn-NxtgAW3fKkEuA_JXRImAR5bz0TDj65EmA/edit?tab=t.yjzrniuwtyyq) and explicitly declines a call, asking for written feedback in the doc. Sets the "shelve if not approvable in writing within a round or two" boundary.

After the email is sent: monitor the Google Doc for Jane's comments. If she approves the v5 structure, build the Pre-launch Supporter Pack:
- Thank-you page upsell card (above or below the FB+IG CTA)
- farmthru.com.au public landing page for the offer
- Stripe checkout flow that issues a discount code on payment success
- The discount code applied at checkout in farmthru.com.au shop

If she pushes back further: defer to user judgment on whether to keep iterating or shelve.

### 2. Stripe rebrand — waiting on FMTH keys (USER ACTION)

Unchanged from PICKUP-2026-04-28: FarmThru sets up own Stripe account, sends keys, we rotate `stripe-farmthru-secret-key` + webhook secret. Full plan in PICKUP-2026-04-28.md item 1. Cloud Scheduler stays PAUSED until Stripe is verified working.

### 3. Three small carryover cleanups (~10 min total)

From the VIP-archive verification doc + variant-build agent reports:

a) Update `sales-skill/scripts/archive_fmth_vip_card.py` regex to also catch `index.html` (close the blind spot that caused the variant=a leak)
b) Cosmetic fixes to `clients/farm-thru/VIP-ARCHIVE-MANIFEST.md` (LP count + line number)
c) Sweep dead `vipDepositBtn` JS handler in `web/templates/campaign_thankyou.html` (binds to a non-existent element, silent failure, harmless but messy)

Bundle into one ~10-min cleanup PR.

### 4. Pytest pollution risk (open follow-up)

`web/tests/test_campaigns.py` uses `app.test_client()` which connects to LIVE prod Firestore via ADC. Running pytest locally pollutes prod with test signups (auto-tagged `is_test=True` by the new filter, but still messy). Recommended fix: `FIRESTORE_EMULATOR_HOST` env var or guard in `_get_db()`.

### 5. Counter doc gap (cosmetic, non-blocking)

Counter doc sits at 128 after the overnight canary signup; public count is 127. Position 128 will be permanently empty until next renumber. Harmless because LP `signupCount` is computed from `is_test != True` query, not the counter.

---

## Verification log

(Agent fills as work resumes)

- [ ] Jane email sent + her response monitored
- [ ] Stripe FMTH keys received + rotated + tested
- [ ] Scheduler resumed + first cycle verified clean (only after Stripe)
- [ ] 3 small carryover cleanups bundled + shipped
- [ ] Birchal v5 outcome: approved → build Supporter Pack | rejected → shelve paid component
- [ ] Pytest pollution fix
- [ ] Counter doc gap reset (next renumber pass)

---

## Don't forget

- Compliance gate (`compliance.enabled: true` in `clients/farm-thru/config.json`) is LIVE on all FMTH content generation
- VIP scheme is ARCHIVED (not deleted) — restoration commands in `clients/farm-thru/VIP-ARCHIVE-MANIFEST.md`
- The pitch doc and the regulatory analysis are SEPARATE — pitch is at `clients/farm-thru/BIRCHAL-PROPOSAL-V2.md` (~1 page, no regs), analysis is at `clients/farm-thru/regulatory/birchal-vip-concerns-regulatory-analysis.md`
- Waitlist counter doc never decrements naturally — manually reset after deletes/renumbers
- Sheet has 8 tabs; only modify Sheet1; range-qualify all writes (`'Sheet1'!A1:Z9999`)
- Cloud Scheduler is in `us-central1` (NOT `australia-southeast1`)
- Always create feature branches; never commit to main

---

## Kickoff prompt (paste into fresh Claude post-compact)

```
Read /Users/jb/Documents/GitHub/learning-loop/clients/farm-thru/PICKUP-2026-04-29.md
in full — read the "2026-04-28 PM session update" section first, it's
the freshest. Then auto-memory at
/Users/jb/.claude/projects/-Users-jb-Documents-GitHub-learning-loop/memory/MEMORY.md.

What's already shipped (DO NOT redo):
- gtmtest variant infra (sales-skill PRs #235 + #236) — verified live
- FMTH email watertight audit + auto-fixer (sales-skill PR #237) —
  19 violations fixed, 0 remaining; reviewer at sales-skill/scripts/audit_fmth_emails.py
- Drip queue is now safe to send when scheduler resumes

Pause for me on:
- Stripe rebrand (waiting on FMTH keys; I'll paste here when in hand)
- Scheduler resume (only after Stripe verified)
- GTM container cutover (I'm validating GTM-53CLCC8X; will tell you when to swap)
- Birchal v5 (waiting on Jane's response in the linked Google Doc)

Default to autonomy on:
- meta.json "Delivery Areas" → "Pickup Areas" copy fix (single line, every LP)
- 3 small carryover cleanups bundled into one PR (manifest text fixes,
  archive_fmth_vip_card.py regex, dead vipDepositBtn JS in thank-you template)
- Pytest pollution risk fix (FIRESTORE_EMULATOR_HOST guard)
- Rubric max_score audit script + CI check
- Writer regression investigation (meta-ad hill-climb scoring 0.000)

If I haven't said what to work on within the first message, start with
the meta.json "Delivery Areas" fix (5 min, ships independently).

Use git worktrees with isolation:"worktree" for code changes per
~/.claude/CLAUDE.md. Squash-merge PRs. Never commit to main.
```
